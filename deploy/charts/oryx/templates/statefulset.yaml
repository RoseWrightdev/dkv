apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "oryx.fullname" . }}
  labels:
    app: {{ include "oryx.fullname" . }}
spec:
  serviceName: {{ include "oryx.fullname" . }}-headless
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "oryx.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "oryx.fullname" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "serve"
          ports:
            - name: grpc
              containerPort: {{ .Values.service.port }}
              protocol: TCP
            - name: resp
              containerPort: {{ .Values.resp.port }}
              protocol: TCP
            - name: syncer
              containerPort: {{ .Values.gossip.port }}
              protocol: TCP
            - name: gossip
              containerPort: {{ .Values.gossip.port }}
              protocol: UDP
          env:
            - name: ORYX_INSECURE
              value: "{{ not .Values.engine.tls.enabled | ternary "true" "false" }}"
            {{- if .Values.engine.tls.enabled }}
            - name: ORYX_TLS_CERT_FILE
              value: "{{ .Values.engine.tls.certFile }}"
            - name: ORYX_TLS_KEY_FILE
              value: "{{ .Values.engine.tls.keyFile }}"
            {{- end }}
            - name: ORYX_NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: ORYX_BIND_ADDR
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: ORYX_ADVERTISE_ADDR
              value: "$(ORYX_BIND_ADDR)"
            - name: ORYX_BIND_PORT
              value: "{{ .Values.gossip.port }}"
            - name: ORYX_GRPC_PORT
              value: "{{ .Values.service.port }}"
            - name: ORYX_RESP_PORT
              value: "{{ .Values.resp.port }}"
            - name: ORYX_REPLICATION_FACTOR
              value: "{{ .Values.engine.replicationFactor }}"
            - name: ORYX_REPLICATION_FAILURE_MODE
              value: "{{ .Values.engine.replicationFailureMode }}"
            - name: ORYX_SEED_NODES
              value: '{{- $replicaCount := int .Values.replicaCount }}{{- $fullName := include "oryx.fullname" . }}{{- $namespace := .Release.Namespace }}{{- $seeds := list }}{{- range $i := until $replicaCount }}{{- $seeds = append $seeds (printf "%s-%d.%s-headless.%s.svc.cluster.local:%d" $fullName $i $fullName $namespace (int $.Values.gossip.port)) }}{{- end }}{{- join "," $seeds }}'
            - name: ORYX_WAL_PATH
              value: "{{ .Values.engine.wal.path }}"
            - name: ORYX_SNP_PATH
              value: "{{ .Values.engine.snapshot.path }}"
            - name: ORYX_WAL_INTERVAL
              value: "{{ .Values.engine.wal.interval }}"
            - name: ORYX_WAL_BUFFER_SIZE
              value: "{{ .Values.engine.wal.bufferSize }}"
            - name: ORYX_WAL_SEGMENTS
              value: "{{ .Values.engine.wal.segments }}"
            - name: ORYX_SNP_INTERVAL
              value: "{{ .Values.engine.snapshot.interval }}"
            - name: ORYX_GOSSIP_INTERVAL
              value: "{{ .Values.engine.gossip.interval }}"
            - name: ORYX_SINGLE_NODE
              value: "{{ .Values.engine.singleNode | ternary "true" "false" }}"
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if not .Values.persistence.enabled }}
      volumes:
        - name: data
          emptyDir: {}
      {{- end }}
  {{- if .Values.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        {{- if .Values.persistence.storageClass }}
        storageClassName: "{{ .Values.persistence.storageClass }}"
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
  {{- end }}
