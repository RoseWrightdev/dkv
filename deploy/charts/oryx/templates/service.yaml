apiVersion: v1
kind: Service
metadata:
  name: {{ include "oryx.fullname" . }}
  labels:
    app: {{ include "oryx.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app: {{ include "oryx.fullname" . }}
