apiVersion: v1
kind: Service
metadata:
  name: {{ include "dkv.fullname" . }}-headless
  labels:
    app: {{ include "dkv.fullname" . }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.gossip.port }}
      targetPort: gossip
      protocol: TCP
      name: gossip
  selector:
    app: {{ include "dkv.fullname" . }}
