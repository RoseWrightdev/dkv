apiVersion: v1
kind: Service
metadata:
  name: {{ include "dkv.fullname" . }}
  labels:
    app: {{ include "dkv.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app: {{ include "dkv.fullname" . }}
