// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.34.1
// source: api/dkv.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "dkv";

export interface PushRequest {
  entries: SetRequest[];
  deletions: DeleteRequest[];
}

export interface PushResponse {
}

export interface PullRequest {
  rootDigest: number;
  shardDigests: { [key: number]: number };
  subDigests: { [key: number]: ShardDigests };
  nodeId: string;
}

export interface PullRequest_ShardDigestsEntry {
  key: number;
  value: number;
}

export interface PullRequest_SubDigestsEntry {
  key: number;
  value: ShardDigests | undefined;
}

export interface ShardDigests {
  subHashes: number[];
}

export interface PullResponse {
  entries: SetRequest[];
  deletions: DeleteRequest[];
}

export interface WalEntry {
  set?: SetRequest | undefined;
  delete?: DeleteRequest | undefined;
}

export interface GetRequest {
  key: string;
}

export interface GetResponse {
  value: Buffer;
  exists: boolean;
}

export interface SetRequest {
  key: string;
  value: Buffer;
  timestamp: number;
  nodeId: string;
}

export interface SetResponse {
}

export interface DeleteRequest {
  key: string;
  timestamp: number;
  nodeId: string;
}

export interface DeleteResponse {
}

function createBasePushRequest(): PushRequest {
  return { entries: [], deletions: [] };
}

export const PushRequest: MessageFns<PushRequest> = {
  encode(message: PushRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      SetRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.deletions) {
      DeleteRequest.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(SetRequest.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deletions.push(DeleteRequest.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushRequest {
    return {
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => SetRequest.fromJSON(e)) : [],
      deletions: globalThis.Array.isArray(object?.deletions)
        ? object.deletions.map((e: any) => DeleteRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PushRequest): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => SetRequest.toJSON(e));
    }
    if (message.deletions?.length) {
      obj.deletions = message.deletions.map((e) => DeleteRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushRequest>, I>>(base?: I): PushRequest {
    return PushRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushRequest>, I>>(object: I): PushRequest {
    const message = createBasePushRequest();
    message.entries = object.entries?.map((e) => SetRequest.fromPartial(e)) || [];
    message.deletions = object.deletions?.map((e) => DeleteRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBasePushResponse(): PushResponse {
  return {};
}

export const PushResponse: MessageFns<PushResponse> = {
  encode(_: PushResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PushResponse {
    return {};
  },

  toJSON(_: PushResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PushResponse>, I>>(base?: I): PushResponse {
    return PushResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushResponse>, I>>(_: I): PushResponse {
    const message = createBasePushResponse();
    return message;
  },
};

function createBasePullRequest(): PullRequest {
  return { rootDigest: 0, shardDigests: {}, subDigests: {}, nodeId: "" };
}

export const PullRequest: MessageFns<PullRequest> = {
  encode(message: PullRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rootDigest !== 0) {
      writer.uint32(9).fixed64(message.rootDigest);
    }
    globalThis.Object.entries(message.shardDigests).forEach(([key, value]: [string, number]) => {
      PullRequest_ShardDigestsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    globalThis.Object.entries(message.subDigests).forEach(([key, value]: [string, ShardDigests]) => {
      PullRequest_SubDigestsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.nodeId !== "") {
      writer.uint32(34).string(message.nodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.rootDigest = longToNumber(reader.fixed64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = PullRequest_ShardDigestsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.shardDigests[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = PullRequest_SubDigestsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.subDigests[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullRequest {
    return {
      rootDigest: isSet(object.rootDigest)
        ? globalThis.Number(object.rootDigest)
        : isSet(object.root_digest)
        ? globalThis.Number(object.root_digest)
        : 0,
      shardDigests: isObject(object.shardDigests)
        ? (globalThis.Object.entries(object.shardDigests) as [string, any][]).reduce(
          (acc: { [key: number]: number }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.shard_digests)
        ? (globalThis.Object.entries(object.shard_digests) as [string, any][]).reduce(
          (acc: { [key: number]: number }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      subDigests: isObject(object.subDigests)
        ? (globalThis.Object.entries(object.subDigests) as [string, any][]).reduce(
          (acc: { [key: number]: ShardDigests }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = ShardDigests.fromJSON(value);
            return acc;
          },
          {},
        )
        : isObject(object.sub_digests)
        ? (globalThis.Object.entries(object.sub_digests) as [string, any][]).reduce(
          (acc: { [key: number]: ShardDigests }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = ShardDigests.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
    };
  },

  toJSON(message: PullRequest): unknown {
    const obj: any = {};
    if (message.rootDigest !== 0) {
      obj.rootDigest = Math.round(message.rootDigest);
    }
    if (message.shardDigests) {
      const entries = globalThis.Object.entries(message.shardDigests) as [string, number][];
      if (entries.length > 0) {
        obj.shardDigests = {};
        entries.forEach(([k, v]) => {
          obj.shardDigests[k] = Math.round(v);
        });
      }
    }
    if (message.subDigests) {
      const entries = globalThis.Object.entries(message.subDigests) as [string, ShardDigests][];
      if (entries.length > 0) {
        obj.subDigests = {};
        entries.forEach(([k, v]) => {
          obj.subDigests[k] = ShardDigests.toJSON(v);
        });
      }
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullRequest>, I>>(base?: I): PullRequest {
    return PullRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullRequest>, I>>(object: I): PullRequest {
    const message = createBasePullRequest();
    message.rootDigest = object.rootDigest ?? 0;
    message.shardDigests = (globalThis.Object.entries(object.shardDigests ?? {}) as [string, number][]).reduce(
      (acc: { [key: number]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.subDigests = (globalThis.Object.entries(object.subDigests ?? {}) as [string, ShardDigests][]).reduce(
      (acc: { [key: number]: ShardDigests }, [key, value]: [string, ShardDigests]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = ShardDigests.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.nodeId = object.nodeId ?? "";
    return message;
  },
};

function createBasePullRequest_ShardDigestsEntry(): PullRequest_ShardDigestsEntry {
  return { key: 0, value: 0 };
}

export const PullRequest_ShardDigestsEntry: MessageFns<PullRequest_ShardDigestsEntry> = {
  encode(message: PullRequest_ShardDigestsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(17).fixed64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullRequest_ShardDigestsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullRequest_ShardDigestsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.value = longToNumber(reader.fixed64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullRequest_ShardDigestsEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: PullRequest_ShardDigestsEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullRequest_ShardDigestsEntry>, I>>(base?: I): PullRequest_ShardDigestsEntry {
    return PullRequest_ShardDigestsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullRequest_ShardDigestsEntry>, I>>(
    object: I,
  ): PullRequest_ShardDigestsEntry {
    const message = createBasePullRequest_ShardDigestsEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBasePullRequest_SubDigestsEntry(): PullRequest_SubDigestsEntry {
  return { key: 0, value: undefined };
}

export const PullRequest_SubDigestsEntry: MessageFns<PullRequest_SubDigestsEntry> = {
  encode(message: PullRequest_SubDigestsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value !== undefined) {
      ShardDigests.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullRequest_SubDigestsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullRequest_SubDigestsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ShardDigests.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullRequest_SubDigestsEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? ShardDigests.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: PullRequest_SubDigestsEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== undefined) {
      obj.value = ShardDigests.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullRequest_SubDigestsEntry>, I>>(base?: I): PullRequest_SubDigestsEntry {
    return PullRequest_SubDigestsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullRequest_SubDigestsEntry>, I>>(object: I): PullRequest_SubDigestsEntry {
    const message = createBasePullRequest_SubDigestsEntry();
    message.key = object.key ?? 0;
    message.value = (object.value !== undefined && object.value !== null)
      ? ShardDigests.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseShardDigests(): ShardDigests {
  return { subHashes: [] };
}

export const ShardDigests: MessageFns<ShardDigests> = {
  encode(message: ShardDigests, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.subHashes) {
      writer.fixed64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShardDigests {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShardDigests();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 9) {
            message.subHashes.push(longToNumber(reader.fixed64()));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.subHashes.push(longToNumber(reader.fixed64()));
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShardDigests {
    return {
      subHashes: globalThis.Array.isArray(object?.subHashes)
        ? object.subHashes.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.sub_hashes)
        ? object.sub_hashes.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ShardDigests): unknown {
    const obj: any = {};
    if (message.subHashes?.length) {
      obj.subHashes = message.subHashes.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShardDigests>, I>>(base?: I): ShardDigests {
    return ShardDigests.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShardDigests>, I>>(object: I): ShardDigests {
    const message = createBaseShardDigests();
    message.subHashes = object.subHashes?.map((e) => e) || [];
    return message;
  },
};

function createBasePullResponse(): PullResponse {
  return { entries: [], deletions: [] };
}

export const PullResponse: MessageFns<PullResponse> = {
  encode(message: PullResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      SetRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.deletions) {
      DeleteRequest.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(SetRequest.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deletions.push(DeleteRequest.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => SetRequest.fromJSON(e)) : [],
      deletions: globalThis.Array.isArray(object?.deletions)
        ? object.deletions.map((e: any) => DeleteRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PullResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => SetRequest.toJSON(e));
    }
    if (message.deletions?.length) {
      obj.deletions = message.deletions.map((e) => DeleteRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullResponse>, I>>(base?: I): PullResponse {
    return PullResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullResponse>, I>>(object: I): PullResponse {
    const message = createBasePullResponse();
    message.entries = object.entries?.map((e) => SetRequest.fromPartial(e)) || [];
    message.deletions = object.deletions?.map((e) => DeleteRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWalEntry(): WalEntry {
  return { set: undefined, delete: undefined };
}

export const WalEntry: MessageFns<WalEntry> = {
  encode(message: WalEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      SetRequest.encode(message.set, writer.uint32(10).fork()).join();
    }
    if (message.delete !== undefined) {
      DeleteRequest.encode(message.delete, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WalEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWalEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = SetRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.delete = DeleteRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WalEntry {
    return {
      set: isSet(object.set) ? SetRequest.fromJSON(object.set) : undefined,
      delete: isSet(object.delete) ? DeleteRequest.fromJSON(object.delete) : undefined,
    };
  },

  toJSON(message: WalEntry): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = SetRequest.toJSON(message.set);
    }
    if (message.delete !== undefined) {
      obj.delete = DeleteRequest.toJSON(message.delete);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WalEntry>, I>>(base?: I): WalEntry {
    return WalEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WalEntry>, I>>(object: I): WalEntry {
    const message = createBaseWalEntry();
    message.set = (object.set !== undefined && object.set !== null) ? SetRequest.fromPartial(object.set) : undefined;
    message.delete = (object.delete !== undefined && object.delete !== null)
      ? DeleteRequest.fromPartial(object.delete)
      : undefined;
    return message;
  },
};

function createBaseGetRequest(): GetRequest {
  return { key: "" };
}

export const GetRequest: MessageFns<GetRequest> = {
  encode(message: GetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRequest {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: GetRequest): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRequest>, I>>(base?: I): GetRequest {
    return GetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRequest>, I>>(object: I): GetRequest {
    const message = createBaseGetRequest();
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseGetResponse(): GetResponse {
  return { value: Buffer.alloc(0), exists: false };
}

export const GetResponse: MessageFns<GetResponse> = {
  encode(message: GetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value.length !== 0) {
      writer.uint32(10).bytes(message.value);
    }
    if (message.exists !== false) {
      writer.uint32(16).bool(message.exists);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.exists = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetResponse {
    return {
      value: isSet(object.value) ? Buffer.from(bytesFromBase64(object.value)) : Buffer.alloc(0),
      exists: isSet(object.exists) ? globalThis.Boolean(object.exists) : false,
    };
  },

  toJSON(message: GetResponse): unknown {
    const obj: any = {};
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    if (message.exists !== false) {
      obj.exists = message.exists;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetResponse>, I>>(base?: I): GetResponse {
    return GetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetResponse>, I>>(object: I): GetResponse {
    const message = createBaseGetResponse();
    message.value = object.value ?? Buffer.alloc(0);
    message.exists = object.exists ?? false;
    return message;
  },
};

function createBaseSetRequest(): SetRequest {
  return { key: "", value: Buffer.alloc(0), timestamp: 0, nodeId: "" };
}

export const SetRequest: MessageFns<SetRequest> = {
  encode(message: SetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    if (message.nodeId !== "") {
      writer.uint32(34).string(message.nodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetRequest {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Buffer.from(bytesFromBase64(object.value)) : Buffer.alloc(0),
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
    };
  },

  toJSON(message: SetRequest): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetRequest>, I>>(base?: I): SetRequest {
    return SetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetRequest>, I>>(object: I): SetRequest {
    const message = createBaseSetRequest();
    message.key = object.key ?? "";
    message.value = object.value ?? Buffer.alloc(0);
    message.timestamp = object.timestamp ?? 0;
    message.nodeId = object.nodeId ?? "";
    return message;
  },
};

function createBaseSetResponse(): SetResponse {
  return {};
}

export const SetResponse: MessageFns<SetResponse> = {
  encode(_: SetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SetResponse {
    return {};
  },

  toJSON(_: SetResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<SetResponse>, I>>(base?: I): SetResponse {
    return SetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetResponse>, I>>(_: I): SetResponse {
    const message = createBaseSetResponse();
    return message;
  },
};

function createBaseDeleteRequest(): DeleteRequest {
  return { key: "", timestamp: 0, nodeId: "" };
}

export const DeleteRequest: MessageFns<DeleteRequest> = {
  encode(message: DeleteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.nodeId !== "") {
      writer.uint32(26).string(message.nodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteRequest {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
    };
  },

  toJSON(message: DeleteRequest): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteRequest>, I>>(base?: I): DeleteRequest {
    return DeleteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRequest>, I>>(object: I): DeleteRequest {
    const message = createBaseDeleteRequest();
    message.key = object.key ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.nodeId = object.nodeId ?? "";
    return message;
  },
};

function createBaseDeleteResponse(): DeleteResponse {
  return {};
}

export const DeleteResponse: MessageFns<DeleteResponse> = {
  encode(_: DeleteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteResponse {
    return {};
  },

  toJSON(_: DeleteResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteResponse>, I>>(base?: I): DeleteResponse {
    return DeleteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteResponse>, I>>(_: I): DeleteResponse {
    const message = createBaseDeleteResponse();
    return message;
  },
};

export type DkvServiceService = typeof DkvServiceService;
export const DkvServiceService = {
  get: {
    path: "/dkv.DkvService/Get" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetRequest): Buffer => Buffer.from(GetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetRequest => GetRequest.decode(value),
    responseSerialize: (value: GetResponse): Buffer => Buffer.from(GetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetResponse => GetResponse.decode(value),
  },
  set: {
    path: "/dkv.DkvService/Set" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetRequest): Buffer => Buffer.from(SetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetRequest => SetRequest.decode(value),
    responseSerialize: (value: SetResponse): Buffer => Buffer.from(SetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetResponse => SetResponse.decode(value),
  },
  delete: {
    path: "/dkv.DkvService/Delete" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteRequest): Buffer => Buffer.from(DeleteRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteRequest => DeleteRequest.decode(value),
    responseSerialize: (value: DeleteResponse): Buffer => Buffer.from(DeleteResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteResponse => DeleteResponse.decode(value),
  },
  /** Anti-Entropy Sync */
  pull: {
    path: "/dkv.DkvService/Pull" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PullRequest): Buffer => Buffer.from(PullRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PullRequest => PullRequest.decode(value),
    responseSerialize: (value: PullResponse): Buffer => Buffer.from(PullResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PullResponse => PullResponse.decode(value),
  },
  push: {
    path: "/dkv.DkvService/Push" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PushRequest): Buffer => Buffer.from(PushRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PushRequest => PushRequest.decode(value),
    responseSerialize: (value: PushResponse): Buffer => Buffer.from(PushResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PushResponse => PushResponse.decode(value),
  },
} as const;

export interface DkvServiceServer extends UntypedServiceImplementation {
  get: handleUnaryCall<GetRequest, GetResponse>;
  set: handleUnaryCall<SetRequest, SetResponse>;
  delete: handleUnaryCall<DeleteRequest, DeleteResponse>;
  /** Anti-Entropy Sync */
  pull: handleUnaryCall<PullRequest, PullResponse>;
  push: handleUnaryCall<PushRequest, PushResponse>;
}

export interface DkvServiceClient extends Client {
  get(request: GetRequest, callback: (error: ServiceError | null, response: GetResponse) => void): ClientUnaryCall;
  get(
    request: GetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetResponse) => void,
  ): ClientUnaryCall;
  get(
    request: GetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetResponse) => void,
  ): ClientUnaryCall;
  set(request: SetRequest, callback: (error: ServiceError | null, response: SetResponse) => void): ClientUnaryCall;
  set(
    request: SetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetResponse) => void,
  ): ClientUnaryCall;
  set(
    request: SetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetResponse) => void,
  ): ClientUnaryCall;
  delete(
    request: DeleteRequest,
    callback: (error: ServiceError | null, response: DeleteResponse) => void,
  ): ClientUnaryCall;
  delete(
    request: DeleteRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteResponse) => void,
  ): ClientUnaryCall;
  delete(
    request: DeleteRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteResponse) => void,
  ): ClientUnaryCall;
  /** Anti-Entropy Sync */
  pull(request: PullRequest, callback: (error: ServiceError | null, response: PullResponse) => void): ClientUnaryCall;
  pull(
    request: PullRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PullResponse) => void,
  ): ClientUnaryCall;
  pull(
    request: PullRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PullResponse) => void,
  ): ClientUnaryCall;
  push(request: PushRequest, callback: (error: ServiceError | null, response: PushResponse) => void): ClientUnaryCall;
  push(
    request: PushRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PushResponse) => void,
  ): ClientUnaryCall;
  push(
    request: PushRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PushResponse) => void,
  ): ClientUnaryCall;
}

export const DkvServiceClient = makeGenericClientConstructor(DkvServiceService, "dkv.DkvService") as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): DkvServiceClient;
  service: typeof DkvServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
